buildscript {
    ext {
        junitVersion = 4.13
    }
}
    
plugins {
    id 'java'
}

repositories {
    flatDir {
        dirs = ['lib']
    }
}

dependencies {
    testImplementation "junit:junit:$junitVersion"
}