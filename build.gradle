// We can specify projects that may applying plugin
['user-service', 'cashier-service'].each { name ->
    project(":$name") {
        apply plugin: 'java'
    }
}

// This will mark that all projects in this multi-project build will be in this version
version = '1.0-SNAPSHOT'